name: CLA Assistant

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

permissions:
  contents: write
  pull-requests: write
  statuses: write

jobs:
  cla_assistant:
    runs-on: ubuntu-latest
    if: |
      (github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA') ||
      (github.event_name == 'pull_request_target' && github.event.action != 'closed')

    steps:
      - name: Ensure cla-signatures branch exists
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const branch = "cla-signatures";
            const ref = `heads/${branch}`;
            const fullRef = `refs/heads/${branch}`;

            try {
              await github.rest.git.getRef({ owner, repo, ref });
              core.info(`${branch} already exists`);
              return;
            } catch (error) {
              if (error.status !== 404) throw error;
            }

            const mainRef = await github.rest.git.getRef({
              owner,
              repo,
              ref: "heads/main",
            });

            await github.rest.git.createRef({
              owner,
              repo,
              ref: fullRef,
              sha: mainRef.data.object.sha,
            });
            core.info(`Created ${branch} at ${mainRef.data.object.sha}`);

      - name: CLA Assistant
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Signatures are stored in a dedicated branch in this repo.
          # A pre-step ensures the branch exists before CLA Assistant runs.
          PERSONAL_ACCESS_TOKEN: ${{ secrets.CLA_TOKEN }}
        with:
          # Path to the CLA document in this repo
          path-to-document: 'https://github.com/kronroe/kronroe/blob/main/CLA.md'

          # Branch and file where signatures are stored
          branch: 'cla-signatures'
          path-to-signatures: 'signatures/cla-v1.json'

          # Message shown on the PR when CLA has not been signed
          custom-notsigned-prcomment: |
            Thank you for contributing to Kronroe! 🎉

            Before your pull request can be merged, please sign our [Contributor Licence Agreement](https://github.com/kronroe/kronroe/blob/main/CLA.md).

            To sign, please read the CLA and post the following comment **exactly** (copy and paste):

            ```
            I have read the CLA Document and I hereby sign the CLA
            ```

          # Message shown after signing
          custom-pr-sign-comment: |
            ✅ Thank you for signing the CLA! Your contribution can now be reviewed.

          # Allowlist — repo owner never needs to sign
          allowlist: rebekahcole,Becky9012

          # Lock PRs from unsigned contributors
          lock-pullrequest-aftermerge: false
